{
  "version": 2,
  "builds": [
    {
      "src": "/*/.*",  // 匹配所有路由
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/.*",
      "dest": "/build/web/$1"  // 匹配Flutter web构建目录
    }
  ]
}